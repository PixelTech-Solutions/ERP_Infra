name: "Deploy ERP Azure - Dev"

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  dev:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra/azure
      environment: dev
      project_name: erp-system
      service_name: azure
    secrets: inherit
