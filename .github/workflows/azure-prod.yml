name: "Deploy ERP Azure - Prod"

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  prod:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra/azure
      environment: prod
      project_name: erp-system
      service_name: azure
    secrets: inherit
