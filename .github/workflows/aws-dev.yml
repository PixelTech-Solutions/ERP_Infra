name: "Deploy ERP AWS - Dev"

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  dev:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra/aws
      environment: dev
      project_name: erp-system
      service_name: aws
    secrets: inherit
